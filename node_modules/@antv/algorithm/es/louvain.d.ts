import { ClusterData, GraphData } from './types';
/**
 * 社区发现 louvain 算法
 * @param graphData 图数据
 * @param directed 是否有向图，默认为 false
 * @param weightPropertyName 权重的属性字段
 * @param threshold
 */
declare const louvain: (graphData: GraphData, directed?: boolean, weightPropertyName?: string, threshold?: number) => ClusterData;
export default louvain;
